# Study configs

Supported configs:

- `paper_suite_smoke.json` — quick end-to-end validation
- `paper_suite.json` — full paper study
- `benchmark_suite.json` — supported benchmark-suite wrapper format

For new work, prefer the canonical paper-suite configs.
